{
  "name": "chisel-notes-server",
  "version": "1.0.0",
  "description": "Express.JS server that creates atomic markdown notes",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Trey Wallis",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  }
}
